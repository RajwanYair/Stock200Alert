name: Deploy to GitHub Pages

on:
  # Primary deployment is Cloudflare Pages (cf-pages.yml).
  # This workflow publishes a GitHub Pages mirror with the SPA at /CrossTide/
  # and the docs site nested at /CrossTide/docs/.
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  pages: write
  id-token: write
  contents: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    name: Build app + docs and deploy
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Build SPA
        # workbox-inject is mandatory: without it sw.js keeps the dev fallback
        # manifest (revision: null) and never picks up new builds.
        run: ./node_modules/.bin/vite build && node scripts/workbox-inject.mjs
        env:
          VITE_BASE: /CrossTide/

      - name: Build docs site
        # Must use the workspace-installed Astro: `npx astro` can fetch a fresh
        # copy from the registry, which then resolves a CommonJS `cookie` and
        # fails with "Named export 'parseCookie' not found".
        run: npm run build --workspace docs-site
        env:
          BASE_URL: /CrossTide/docs/

      - name: Merge docs into Pages artifact
        run: |
          mkdir -p dist/docs
          cp -R docs-site/dist/. dist/docs/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5.0.1
