# CrossTide — API key for market data provider (optional with Yahoo Finance)
# Copy this to .env and fill in your key. NEVER commit .env to git.
MARKET_DATA_API_KEY=your_api_key_here
MARKET_DATA_PROVIDER=alpha_vantage
